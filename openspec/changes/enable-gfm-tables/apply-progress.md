# Apply Progress: enable-gfm-tables

**Date**: 2026-08-07  
**Mode**: Standard (strict_tdd: false)

## Completed Tasks

- [x] 1.1 `npm install remark-gfm` — `remark-gfm` ^4 added to `package.json` and `package-lock.json`
- [x] 1.2 **Verification-first** — ESM heredoc test: T2 (wrapped table) **PASS**, top-level table **PASS** — GFM tables parse correctly before wiring
- [x] 2.1 `page.tsx` — import `remarkGfm`, set `remarkPlugins: [remarkMath, remarkGfm]` (math-first ordering)
- [x] 2.2 `lesson08.md` L90 — `\<10,000` (CommonMark backslash escape, exact spec form)
- [x] 2.3 `page.tsx` L171 — H1 strip regex `/^\s*# .+\n?/` for leading-newline titles
- [x] 3.1 `npm run type-check` — exit 0, no errors
- [x] 3.2 CompileMDX audit — **48/48 lessons OK, 0 RAW_TABLE, 0 COMPILE_ERROR**
- [x] 3.3 `git status` — only 4 expected files changed (pre-existing branch diffs in .atl/, .gitignore, tsconfig.json, openspec/config.yaml are not from this apply)

## Verification Results

| Gate | Command | Result |
|------|---------|--------|
| Type-check | `npx tsc --noEmit` | Exit 0, zero errors |
| CompileMDX audit | 48 lessons scanned | 0 RAW_TABLE, 0 COMPILE_ERROR |
| Build | `npm run build` | **Deferred to Vercel** (user instruction, W-BUILD-DEFERRED) |

### T2 Verification-First Test Output

```
T2 (wrapped table): PASS: table inside wrapper
Top-level table: PASS: table detected
VERDICT: ALL PASS
```

## Files Changed

| File | Action | Description |
|------|--------|-------------|
| `package.json` | Modified | Added `"remark-gfm": "^4.0.0"` to dependencies |
| `package-lock.json` | Modified | Auto-generated by npm install |
| `src/app/learn/[module]/[slug]/page.tsx` | Modified | Import `remarkGfm`, add to `remarkPlugins`, fix H1 regex |
| `src/content/modules/machine-learning/lessons/lesson08_gradient_boosting/lesson.md` | Modified | Escape `<10,000` → `\<10,000` at line 90 |

## Deviations from Design

None — implementation matches design exactly.

## Issues Found

None.

## Work Unit Evidence

| Evidence | Value |
|----------|-------|
| Focused test command | `npx tsc --noEmit` — exit 0 |
| Runtime harness | compileMDX audit (48 lessons) — 0 RAW_TABLE, 0 COMPILE_ERROR |
| Rollback boundary | Revert page.tsx edits + lesson08.md edit + `npm uninstall remark-gfm`; no migration, no schema change |
